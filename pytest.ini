[pytest]
addopts = -rsxX -l --tb=short --strict --pylint --black --cov=redcap
# Keep current format for future version of pytest
junit_family=xunit1
# Ignore unimportant warnings
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
